// REPL

// R -> Read
// E -> Eval
// P -> Print
// L -> Loop

// provides an env like windows console where we can put commands and run them

// reads the input from user -> Parse it in JS Data structure -> store it in memory
// -> executes or evaluate the data structure -> then prints the result

// to stop press CTRL + C 2 times
